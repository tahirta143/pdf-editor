name: pdfeditorapp
description:
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons:
  curved_navigation_bar:
  url_launcher:
  file_picker: ^8.1.2
  printing: ^5.14.3
  syncfusion_flutter_pdfviewer: ^33.2.5
  syncfusion_flutter_pdf: ^33.2.5
  syncfusion_flutter_signaturepad: ^33.2.5
  pdf: ^3.10.7
  path_provider:
  open_file:
  http:
  path:
  share_plus:
  permission_handler:



dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  flutter_launcher_icons:

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png"


flutter:

  assets:
    - assets/images/

  uses-material-design: true
